/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: false,
  images: {
    domains: ["9furg9ixi3wjuc9w.public.blob.vercel-storage.com"],
  },
};

export default nextConfig;
